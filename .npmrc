# pnpm configuration for Leave Sprint Twin
# Use hoisted node_modules for maximum compatibility with some packages (e.g. sharp, esbuild, etc.)
node-linker=hoisted

# Strictness (recommended)
strict-peer-dependencies=false
# auto-install-peers=true
